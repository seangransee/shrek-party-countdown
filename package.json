{
  "name": "shrek-party-countdown",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "moment": "^2.24.0",
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm -rf docs && react-scripts build && mv build docs && echo shrekpartycountdown.com > docs/CNAME",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "npm run build && git add . && git commit -am 'publish' && git push origin master"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
